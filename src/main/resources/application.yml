spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wallet_db?TimeZone=UTC
    username: wallet_user
    password: wallet_pass
    jackson:
      time-zone: UTC

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        dialect: org.hibernate.dialect.PostgreSQLDialect


  mail:
    host: smtp.gmail.com
    port: 587
    username: hoanganhquan977584@gmail.com
    password: ifvo xejb zztz bqbg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  docker:
    compose:
      enabled: false
server:
  port: 8081

